# 📌 Spring Boot 게시판 REST API 실습 프로젝트 (rest)

이 프로젝트는 Spring Boot, Spring Data JPA를 기반으로 구축된 **게시판 REST API** 애플리케이션 실습 저장소입니다.  
게시글의 생성, 조회, 수정, 삭제(CRUD) 기능과 예외 처리, CORS 설정, 그리고 Swagger를 이용한 API 문서화가 적용되어 있습니다.

---

## 🛠 기술 스택
- **Framework**: Spring Boot 3.x
- **Database**: MySQL (Aiven Cloud 활용)
- **ORM / JPA**: Spring Data JPA, Hibernate
- **Build Tool**: Gradle
- **Documentation**: SpringDoc OpenAPI (Swagger UI)
- **Utility**: Lombok

---

## 📂 프로젝트 패키지 구조
```text
src/main/java/org/example/rest/
├── RestApplication.java       # 애플리케이션 진입점
├── config/
│   ├── SwaggerConfig.java     # OpenAPI / Swagger UI 설정
│   └── WebConfig.java         # CORS 전역 설정 (WebMvcConfigurer)
├── controller/
│   ├── BoardController.java   # 게시판 CRUD REST 컨트롤러
│   └── MyController.java      # 단순 테스트용 REST 컨트롤러
├── domain/
│   └── entity/
│       ├── BaseEntity.java    # 등록/수정 시간 JPA Auditing 기반 추상 클래스
│       └── BoardEntity.java   # 게시글 JPA 엔티티 (UUID 기반 외부 식별자 제공)
├── dto/
│   ├── BoardRequestDTO.java   # 게시글 생성/수정 요청 DTO (Bean Validation 적용)
│   └── BoardResponseDTO.java  # 게시글 정보 반환 DTO
├── exception/
│   ├── GlobalControllerAdvice.java             # 커스텀 예외 전역 처리기
│   ├── GlobalControllerAdviceProblemDetail.java # RFC 7807 Problem Details 기반 예외 처리기
│   └── NotFoundedException.java                # 404 예외 정의 클래스
└── service/
    └── BoardService.java      # 게시판 비즈니스 로직
```

---

## 📡 REST API 엔드포인트 명세
모든 요청 및 응답은 `application/json` 형식으로 처리됩니다.

| HTTP 메서드 | URI | 설명 | 주요 HTTP 상태 코드 |
| :--- | :--- | :--- | :--- |
| **POST** | `/board` | 게시글 생성 | `201 Created` / `400 Bad Request` |
| **GET** | `/board` | 게시글 전체 목록 조회 | `200 OK` |
| **GET** | `/board/{uuid}` | 특정 게시글 상세 조회 | `200 OK` / `404 Not Found` |
| **PUT** | `/board/{uuid}` | 특정 게시글 전체 수정 | `200 OK` |
| **PATCH** | `/board/{uuid}/title` | 특정 게시글 제목 수정 | `200 OK` |
| **DELETE** | `/board/{uuid}` | 특정 게시글 삭제 | `204 No Content` |
| **GET** | `/board/error` | 고의 에러 발생 테스트 | `500 Internal Server Error` |

---

## 💡 주요 구현 특징

### 1. 외부 노출용 UUID 활용
- 데이터베이스 기본 키(`id`)로 Auto-Increment `Long`을 사용하되, 보안과 유추 방지를 위해 외부에 공개되는 엔드포인트는 `UUID` 기반 식별자(`uuid`)를 활용하여 데이터를 조회 및 관리합니다.

### 2. JPA Auditing 기능 구현
- [`BaseEntity.java`](file:///C:/workspace/rest/src/main/java/org/example/rest/domain/entity/BaseEntity.java)에 `@CreatedDate` 및 `@LastModifiedDate`를 적용하여 모든 테이블의 생성 시간과 최종 수정 시간이 자동으로 기록되도록 하였습니다.

### 3. RFC 7807 (Problem Details) 표준 에러 응답
- 예외 발생 시 표준 규격에 맞추어 클라이언트에 응답을 주고자 [`GlobalControllerAdviceProblemDetail.java`](file:///C:/workspace/rest/src/main/java/org/example/rest/exception/GlobalControllerAdviceProblemDetail.java)를 통해 `ProblemDetail` 표준 객체 형식으로 에러 정보와 메세지를 정교하게 리턴합니다.

### 4. CORS 전역 설정 및 정적 테스트 페이지 제공
- 외부 프론트엔드 환경에서의 자유로운 테스트를 위해 [`WebConfig.java`](file:///C:/workspace/rest/src/main/java/org/example/rest/config/WebConfig.java)에 글로벌 CORS 필터를 정의하여 특정 오리진(`http://localhost:5500`, `http://127.0.0.1:5500`)에 대해 API 접근 권한과 캐싱 시간을 제공합니다.
- [`src/main/resources/static/index.html`](file:///C:/workspace/rest/src/main/resources/static/index.html)에 CORS 및 비동기 Fetch 동작 여부를 직접 테스트할 수 있는 웹 페이지가 마련되어 있습니다.

### 5. Swagger UI 연동을 통한 대화형 API 문서
- 개발(Dev) 프로필 구동 시 `springdoc-openapi`가 적용되어 API의 상세 동작과 설명을 대화형 UI로 확인할 수 있습니다.

---

## 🚀 실행 및 실행 방법

### 1. 환경 설정 파일 생성
프로젝트 루트 디렉토리에 `.env.dev` 파일을 생성하고 아래 형식을 바탕으로 데이터베이스 접속 세부 환경 정보를 입력합니다:
```properties
DB_HOST={MySQL 호스트 주소}
DB_PORT={MySQL 포트 번호}
DB_NAME={데이터베이스 이름}
DB_USERNAME={데이터베이스 사용자 아이디}
DB_PASSWORD={데이터베이스 사용자 비밀번호}
```

### 2. 애플리케이션 실행
프로젝트 루트 폴더에서 아래 Gradle 명령어를 이용하여 애플리케이션을 구동합니다:

**Windows PowerShell:**
```powershell
./gradlew bootRun
```

**Linux / macOS:**
```bash
chmod +x gradlew
./gradlew bootRun
```

### 3. API 문서 확인 (Swagger UI)
애플리케이션이 실행되면 브라우저에서 아래 주소로 접속하여 API 문서 확인 및 직접 테스트(Try it out)가 가능합니다:
- **Swagger UI 주소**: `http://localhost:8080/swagger-ui/index.html`

<!-- pdf-til-supplement:start -->
## TIL 부연 설명 — PDF와 연결하기

기존 실습 내용을 이해하기 위한 PDF 기반 부연 설명이다. 아래 예시는 개념을 설명하기 위한 것이며, 이 프로젝트에서 실행해 관찰한 결과와는 구분한다. 페이지 번호는 표지를 포함한 PDF 순서다.

함께 읽을 파일: [src/main/java/org/example/rest/controller/BoardController.java](<../../rest/src/main/java/org/example/rest/controller/BoardController.java>) · [src/main/java/org/example/rest/controller/MyController.java](<../../rest/src/main/java/org/example/rest/controller/MyController.java>) · [src/main/java/org/example/rest/RestApplication.java](<../../rest/src/main/java/org/example/rest/RestApplication.java>)

### 자원·HTTP 상태·DTO를 하나의 계약으로

REST API는 화면 파일 대신 자원과 행동을 HTTP 메서드·경로·상태 코드·본문으로 표현한다. DTO를 사용하면 엔티티의 내부 필드나 연관관계 변화가 그대로 외부 계약이 되는 일을 줄인다. 성공 응답뿐 아니라 없는 자원과 잘못된 입력도 같은 계약의 일부다.

**예시로 이해하기:** 도서 생성은 POST /books, 단건 조회는 GET /books/{id}처럼 생각할 수 있다. 생성 성공의 201과 새 자원의 위치, 본문 없는 204를 구분한다. 클라이언트가 204에도 무조건 JSON 파싱을 하면 정상 응답을 오류로 처리할 수 있다.

근거: 421-1 REST API 기초 — [9쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=9>) · [18쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=18>) · [19쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=19>) · [35쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=35>) · [42쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=42>) · [43쪽](<../../260629_ex/새 폴더/8-13/421-1_REST_API_기초.pdf#page=43>)

### 오류 응답을 클라이언트가 처리할 수 있게 만들기

REST의 오류 응답은 상태 코드와 일관된 본문이 함께 있어야 한다. ProblemDetail의 type·title·status·detail·instance는 오류의 종류와 상황을 표현하고 필요한 필드 오류는 확장 정보로 추가할 수 있다. 도메인 예외를 HTTP 응답으로 바꾸는 책임을 공통 처리기에 모으면 중복을 줄인다.

**예시로 이해하기:** 없는 글은 404, 입력 검증 실패는 400처럼 클라이언트가 대응을 구분할 수 있게 한다. 서버의 SQL·스택 추적·비밀 설정을 응답에 담지 않고 요청 식별자로 로그와 연결한다. 실제 HTTP 상태와 본문의 status가 일치하는지도 확인한다.

근거: 421-2 REST API 예외 처리와 문서화 — [9쪽](<../../260629_ex/새 폴더/8-13/421-2_REST_API_예외_처리와_문서화.pdf#page=9>) · [17쪽](<../../260629_ex/새 폴더/8-13/421-2_REST_API_예외_처리와_문서화.pdf#page=17>) · [18쪽](<../../260629_ex/새 폴더/8-13/421-2_REST_API_예외_처리와_문서화.pdf#page=18>) · [31쪽](<../../260629_ex/새 폴더/8-13/421-2_REST_API_예외_처리와_문서화.pdf#page=31>) · [39쪽](<../../260629_ex/새 폴더/8-13/421-2_REST_API_예외_처리와_문서화.pdf#page=39>)

### CORS는 브라우저의 응답 접근 규칙

출처는 scheme·host·port의 조합이다. CORS는 브라우저가 다른 출처의 응답을 자바스크립트에 공개해도 되는지 판단하도록 서버가 허용 정보를 보내는 방식이며 사용자 인증을 대신하지 않는다. 일부 요청은 실제 요청 전에 OPTIONS 사전 요청으로 허용 여부를 확인한다.

**예시로 이해하기:** 터미널 요청은 성공하고 브라우저에서 실패한다면 사전 요청과 응답 헤더를 확인한다. 쿠키를 사용하는 교차 출처 요청은 클라이언트의 credentials 설정과 서버의 명시적 출처 허용이 함께 필요하다. 단순 요청은 서버에 도달한 뒤 응답 읽기만 차단될 수도 있다.

근거: 422 CSR 연동과 CORS — [14쪽](<../../260629_ex/새 폴더/8-14/422_CSR_연동과_CORS.pdf#page=14>) · [18쪽](<../../260629_ex/새 폴더/8-14/422_CSR_연동과_CORS.pdf#page=18>) · [23쪽](<../../260629_ex/새 폴더/8-14/422_CSR_연동과_CORS.pdf#page=23>) · [24쪽](<../../260629_ex/새 폴더/8-14/422_CSR_연동과_CORS.pdf#page=24>) · [28쪽](<../../260629_ex/새 폴더/8-14/422_CSR_연동과_CORS.pdf#page=28>)

<!-- pdf-til-supplement:end -->
