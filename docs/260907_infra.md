# Docker와 Linux 기초 명령어 실습

> 2026-09-07에 Docker 컨테이너를 실행해 Ubuntu 환경에서 Linux 기본 명령어, 환경 변수, 파일 권한, 배포 스크립트 실행을 실습했다.

## Docker 기본 확인과 이미지 준비

```sh
docker -v
docker version
docker info

docker pull ubuntu:26.04
docker images
```

- `docker -v`: Docker의 간단한 버전을 확인한다.
- `docker version`: 클라이언트와 서버의 상세 버전을 확인한다.
- `docker info`: Docker 엔진, 컨테이너, 이미지 등의 현재 상태를 확인한다.
- `docker pull ubuntu:26.04`: Ubuntu 26.04 이미지를 내려받는다.
- `docker images`: 로컬 이미지 목록을 확인한다.

## 컨테이너 생성, 분리, 재접속

```sh
docker run -it --name linux-practice ubuntu:26.04 bash

# 컨테이너를 종료하지 않고 분리: Ctrl + P, Ctrl + Q
docker ps
docker attach linux-practice
```

- `docker run`: 컨테이너를 생성하고 실행한다.
- `-i`: 표준 입력을 유지한다.
- `-t`: 터미널을 할당한다.
- `--name linux-practice`: 컨테이너 이름을 지정한다.
- `docker ps`: 실행 중인 컨테이너를 확인한다. 종료된 컨테이너까지 보려면 `docker ps -a`를 쓴다.
- `docker attach`: 분리된 컨테이너의 터미널에 다시 연결한다.
- `docker rm linux-practice`: 중지된 컨테이너를 삭제한다.

## 패키지와 파일 시스템

```sh
apt update
apt install -y curl
curl --version

pwd
ls -la
mkdir /practice
cd /practice
```

- `apt update`: 설치 가능한 패키지 목록을 최신으로 갱신한다.
- `apt install -y curl`: 확인 질문 없이 curl을 설치한다.
- `pwd`: 현재 작업 경로를 출력한다.
- `ls -la`: 숨김 파일(`-a`)을 포함해 상세 정보(`-l`)를 출력한다.
- `mkdir`: 폴더를 만들고, `cd`는 폴더로 이동한다.

## 환경 변수와 파일 리다이렉션

```sh
export APP_ENV=development
export PORT=8080
printenv APP_ENV
echo $PORT

echo "APP_ENV=development" > app.env
echo "PORT=8080" >> app.env
cat app.env
```

- `export`: 현재 셸과 하위 프로세스에서 쓸 환경 변수를 설정한다.
- `printenv`, `echo $변수명`: 환경 변수 값을 확인한다.
- `>`: 파일을 만들거나 기존 내용을 덮어쓴다.
- `>>`: 파일 끝에 내용을 추가한다.
- `cat`: 파일 전체 내용을 출력한다.

## 배포 스크립트 다운로드와 검색

```sh
curl -L -o deploy.sh https://gist.githubusercontent.com/qus0in/d36165612fdfdb1e3622924ef37d6b46/raw/2d9424734b5f8ef8e8b6cd3871a0ce6740160b51/deploy.sh

head -n 3 deploy.sh
tail -n 3 deploy.sh
grep -n "echo" deploy.sh
cat deploy.sh | grep -i "server"
```

- `curl -L`: 리다이렉트된 주소도 따라가서 내려받는다.
- `-o deploy.sh`: 내려받은 내용을 지정한 파일 이름으로 저장한다.
- `head -n 3`, `tail -n 3`: 파일의 처음 또는 마지막 3줄을 확인한다.
- `grep -n`: 일치하는 줄과 줄 번호를 출력한다.
- `grep -i`: 대소문자를 구분하지 않고 검색한다.
- `|`: 앞 명령어의 결과를 다음 명령어의 입력으로 전달하는 파이프다.

## 실행 권한과 스크립트 실행

```sh
chmod +x deploy.sh
./deploy.sh

chmod 744 app.env
ls -l
```

- `chmod +x`: 파일에 실행 권한을 추가한다.
- `./deploy.sh`: 현재 폴더의 스크립트를 직접 실행한다. 실행 권한이 없다면 `sh deploy.sh`로 실행할 수도 있다.
- `chmod 744 app.env`: 소유자에게 읽기·쓰기·실행 권한(7), 그룹과 다른 사용자에게 읽기 권한(4)을 부여한다.
- `ls -l`: 권한 변경 결과를 확인한다.

## 정리

Docker 컨테이너는 격리된 Linux 실습 환경을 빠르게 준비할 수 있게 해 준다. 컨테이너 안에서 패키지 설치, 환경 변수 설정, 파일 작성, 텍스트 검색, 실행 권한 부여까지 기본적인 서버 작업 흐름을 한 번에 연습했다.

원본 실습 저장소: [260907_infra](https://github.com/pjmoo/260907_infra)

<!-- pdf-til-supplement:start -->
## TIL 부연 설명 — PDF와 연결하기

기존 실습 내용을 이해하기 위한 PDF 기반 부연 설명이다. 아래 예시는 개념을 설명하기 위한 것이며, 이 프로젝트에서 실행해 관찰한 결과와는 구분한다. 페이지 번호는 표지를 포함한 PDF 순서다.

**현재 실습과 연결:** 현재 추가된 PDF에는 Docker·Linux 전용 차시가 없다. 아래 DB 접속 환경과 설정 외부화는 컨테이너 실습의 연결 개념이며, Docker 명령 자체를 설명하는 원문으로 제시한 것은 아니다.

### DB 클라이언트와 DB 서버 구분하기

DBeaver 같은 클라이언트는 SQL을 보내고 결과를 보여 주며 실제 데이터 저장과 쿼리 실행은 DB 서버가 맡는다. 연결에는 호스트·포트·데이터베이스·계정 등 서로 다른 정보가 필요하다. 관리형 DB를 사용해도 애플리케이션의 스키마·쿼리·권한 설계는 남아 있다.

**예시로 이해하기:** 연결 실패 시 서버에 도달하지 못하는 문제와 인증 실패, 존재하지 않는 DB 선택을 구분한다. localhost는 실행 중인 환경 자신을 가리키므로 컨테이너 안과 호스트에서 같은 문자열이 같은 서버를 뜻한다고 가정하면 안 된다.

근거: 301-2 데이터베이스 실습 — [7쪽](<../../260629_ex/새 폴더/7-9/301-2_데이터베이스_실습.pdf#page=7>) · [11쪽](<../../260629_ex/새 폴더/7-9/301-2_데이터베이스_실습.pdf#page=11>) · [16쪽](<../../260629_ex/새 폴더/7-9/301-2_데이터베이스_실습.pdf#page=16>) · [18쪽](<../../260629_ex/새 폴더/7-9/301-2_데이터베이스_실습.pdf#page=18>)

### 설정 파일의 값과 실제 적용값 구분하기

같은 설정 키가 여러 출처에 있으면 우선순위에 따라 최종 값이 정해진다. 프로파일은 환경별 설정을 선택하고 ConfigurationProperties는 관련 값을 타입으로 묶는다. 파일에 값이 적혀 있다는 사실만으로 그 값이 실행 시 사용된다고 판단하면 설정 오류를 놓칠 수 있다.

**예시로 이해하기:** 개발 포트를 바꿨는데 반영되지 않으면 활성 프로파일과 환경 변수·실행 인자를 함께 확인한다. .env 파일도 존재만으로 모든 실행 도구에 자동 적용되는 것은 아니므로 import나 로딩 구성을 읽는다. 비밀 값 자체보다 어떤 설정 키가 어디서 공급되는지를 TIL에 남긴다.

근거: 401-2 application-yml과 외부 설정 — [10쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=10>) · [13쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=13>) · [17쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=17>) · [28쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=28>) · [32쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=32>) · [34쪽](<../../260629_ex/새 폴더/8-4/401-2_application-yml과_외부_설정.pdf#page=34>)

<!-- pdf-til-supplement:end -->
